<odoo>
    <data>
      
    <record id="sale.action_orders" model="ir.actions.act_window">
       
        <field name="context">{}</field> 
        <field name="domain">[]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new sale!
            </p><p>
                Once the quotation is confirmed, it becomes a sales order.<br/> You will be able to create an invoice and collect the payment.
            </p>
        </field>
    </record>
     <!-- <field name="inherit_id" ref="stock.product_search_form_view_stock_report"/> -->
    <record id="onhand_zero_qty_check" model="ir.ui.view">
        <field name="name">onhand_zero_qty_check</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <search>
                <filter string="Product On Hand" name="filter_qty_available" domain="[('qty_available', '>', 0)]" help="Not On Hand Zero Products"/>
            </search>
        </field>
    </record>

    <record id="product_normal_action_sell" model="ir.actions.act_window">
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">product.product</field>
        <!-- <field name="view_mode">tree,form,kanban</field> -->
        <!-- <field name="view_type">tree</field> -->
        <field name="context">{"search_default_filter_qty_available":1}</field>
        <!-- <field name="view_id" ref="stock.product_product_stock_tree"/>   -->
        <!-- <field name="search_view_id" ref="stock.product_search_form_view_stock_report"/>             -->
        <field name="help">You must define a Product for everything you buy or sell. Products can be raw materials, stockable products, consumables or services. The Product form contains detailed information about your products related to procurement logistics, sales price, product category, suppliers and so on.</field>
    </record>
    

    </data>
</odoo>
